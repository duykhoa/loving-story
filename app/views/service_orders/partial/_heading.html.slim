#owl-carousel.owl-carousel.owl-theme
  - 3.times do |i|
    .item style=("background: url(#{asset_url("service_orders/carousel-#{i+1}.jpg")}) no-repeat center top;")
      .overlay
      .content
        h1.title
          | Adam
          = icon 'heart'
          | Eve
        .welcome
          | Welcome to adams' & Eves' website where we proudly tell and recored our love story with all its ups and downs with all great moments and happy events. Love Moments are special and it deserve to be recorded and to be always remembered! our loveline is just summerize some of our important and happy moments.
        = link_to "Discover More", '#', class: "button button-primary"
nav
  .container
    .row
      .two.columns.logo
        = image_tag('service_orders/logo.png')
      .ten.columns
        .wrapper.u-pull-right
          ul
            - ["Adma & Eve", "Our family", "Big moment", "favorite list", "Our blog", "Contact"].each do |f|
              li
                = link_to f, "#"

javascript:
  $("#owl-carousel").owlCarousel({
      singleItem:true,
      autoPlay: true
  });

  $(".item").css("height", $(window).height())
