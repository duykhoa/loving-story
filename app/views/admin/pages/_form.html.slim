= simple_form_for [:admin, @dynamic] do |f|
  .form-group
    = f.input :name, input_html: { class: 'form-control' }
  .form-group
    = f.input :content, class: 'summernote'
  .form-group
    = f.button :submit, class: "btn-primary"
